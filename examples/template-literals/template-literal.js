/* interpolation and expression evaluation */
const name = 'Todd';
const smile = true;

console.log(`Hi ${name}!  ${smile ? 'Nice to see you!' : 'Aww, what\'s wrong?'}`)

/* multiline strings */
const ml = `
Finally!
A decent way to do multiline strings in JS!
`;

console.log(ml);

